Apply six review fixes to CodeBlock.vue — four blind reviewers (CC, Codex, CodeRabbit, SonarQube) all flagged the same issues.

READ FIRST: `.cc-dispatch/loops/loop-003-review-fixes/goal.md` — full spec with exact code for each fix.

Mode: autonomous (--dangerously-skip-permissions). Work directly on `main`.
Do NOT touch the GOTO 2026 masterclass deck or any other repo.

Done when: `bun run build` exits 0; all 12 story slides render; Go story unclipped; no-chrome story font ≤ 48px; JSON + HTML temp slides render with colour; bad-lang fallback shows plaintext (not blank); `shiki` in devDependencies — or stop after 30 turns and report what's blocking.

Stop: write `report.md`, commit `fix: apply four-source review fixes (F1-F6)`, notify, STOP.
